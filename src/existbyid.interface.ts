export interface StaffExist{
    exist: boolean;
    name: string | null;
}
export interface RoleExist{
    exist: boolean;
    name: string | null;
}
export interface CustomerExistForBooking{
    exist: boolean;
    name: string | null;
    phone: string | null;
}
